# archive/

Frozen historical artifacts. Things we never want to lose but that should not
appear in semantic search or active development context.

Move files here instead of deleting when you want to preserve provenance.
